server:
  port: ${port:8080}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/boot_chat
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      naming:
        strategy: org.hibernate.cfg.EJB3NamingStrategy

